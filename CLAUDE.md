# CLAUDE.md｜索引路由（不是內容本體）

> 這是一套給未來「較弱模型」（Sonnet / Haiku 這個等級）長期使用的 Claude Code 操作制度。
> 這個 repo（`JoshTseng777/JoshTseng777`）是這套制度的主控中心兼通用範本庫——repo 本體目前
> 只是一份 GitHub profile README，還沒有實際專案內容，所以下面這些規則寫成通用版，之後
> 套到別的專案也能用。
>
> 本檔**只負責指路**，不重複別的檔案內容。看到某個情境，就去讀對應那份檔案的全文，
> 讀完再動手；不要只憑這裡的一句話說明就開始做事。

## 索引表：什麼情境該讀哪一份

| 路徑 | 一句話說明 | 什麼情境該去讀 |
|---|---|---|
| `docs/ops/G-handoff.md` | 交接檔，全程 append 的進度/假設/風險紀錄 | **任何 session 一開始動手前**都先讀這份，不要憑猜測接續任務 |
| `docs/ops/A-diagnosis.md` | 這個 harness 最容易出錯的前三名快速診斷 | 任務開始前先掃一眼，提醒自己容易踩的三個坑 |
| `docs/ops/C-model-dispatch.md` | 模型調度守則：何時派 subagent、交辦三要素、模型 ID 對照表、回報合約、升降級路徑 | 要決定「這件事自己做還是派給誰、派給哪個模型」時讀 |
| `docs/ops/D-judgment-rubrics.md` | 判斷力外化：何時升級模型/何時算完成/何時該問使用者/何時該換路/品質底線怎麼驗 | 猶豫「這樣算不算做完」「要不要停下來問人」時讀 |
| `docs/ops/F-maintenance.md` | 維護協議：哪些檔案能自己改、哪些要先問、踩雷教訓怎麼寫回、多長要精簡 | 要修改這套制度本身的檔案時，先讀這份確認自己有沒有權限直接改 |
| `evidence/README.md` | evidence/ 資料夾使用說明（目前是空資料夾） | 遇到真實失敗案例要存檔、或想確認 A/D 的推測有沒有實例可查時讀 |
| `docs/ops/E-templates/search.md` | 「搜尋類」任務交辦 prompt 範本 | 要交辦一個搜尋/定位類任務給 subagent 時，照這份範本寫 prompt |
| `docs/ops/E-templates/implement.md` | 「實作類」任務交辦 prompt 範本 | 要交辦一個寫程式/改功能類任務時，照這份範本寫 prompt |
| `docs/ops/E-templates/refactor.md` | 「重構類」任務交辦 prompt 範本 | 要交辦一個重構/清理程式碼類任務時，照這份範本寫 prompt |
| `docs/ops/E-templates/research.md` | 「研究類」任務交辦 prompt 範本 | 要交辦一個開放式調查/比較方案類任務時，照這份範本寫 prompt |
| `docs/ops/E-templates/review.md` | 「審查類」任務交辦 prompt 範本 | 要交辦一個驗收/審核他人產出類任務時，照這份範本寫 prompt |

> 上面五份 `E-templates/*.md` 若當下還讀不到內容，代表還在撰寫中，先照本表的路徑和說明
> 使用，不用等它們寫完才動手。

## 隨時該記住的操作原則（細節都在對應檔案裡，這裡只列一句話）

1. 動手前先讀 `G-handoff.md`，接續任務進度，不要重讀整段舊對話瞎猜。
2. 指揮官不下場：大量讀取、掃 repo、批次改檔、跑驗證，一律派便宜的 subagent 去做。
3. 交辦任務一定講清楚三件事：目標與動機、驗收條件、回報格式（細節見 `C-model-dispatch.md`）。
4. 模型名稱、API 參數這類事實，當下環境能查就查，查不到就照實寫「未確認」，不要憑記憶填。
5. 驗證不自驗：執行任務的 agent 不能是驗證自己成果的那個 agent。
6. 小模型錯一次就升級，不在同一等級重試第二次；同一件事最多重試兩輪，兩輪不行就停下來問使用者。
7. 遇到真實踩雷案例，先存進 `evidence/`，累積到 2 筆以上同類案例再回頭更新 A/C/D。
8. 規則是服務任務的：同一條規則連續三次讓你繞了無謂遠路，就標記「待刪」寫進 G 檔，不要無條件死守。
